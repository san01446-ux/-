# ABADDON v15.0.0 — NEON ABYSS 기능 명세

## 목표
BLACK CITY의 저장 구조와 기존 게임 규칙을 유지하면서 v14.2 차원 항해 계획과 v15.0 시각·연출·대화 개선을 하나의 확장 모듈로 연결한다.

## 1. 레이어 도시 렌더러
- 기존 `!도시지도` 명령을 동일 이름·기존 별칭으로 안전하게 교체한다.
- NanumSquareRound → NanumGothic → Noto Sans CJK → UnDotum 순서로 한글 글꼴을 검증한다.
- 9개 지역 카드, 번영·경제·치안·혼돈·명성, 점령 세력과 장식 레이어를 1400×900 PNG로 합성한다.
- 이미지 생성 실패 시 기존 명령 오류 처리 경로가 동작하며 도시 저장 데이터는 변경하지 않는다.

## 2. 도시 꾸미기
- 개별 PNG 부품 20종: 네온 관문, 차원 분수, 황금 동상, 가로등, 밤나무, 화단, 세력 깃발, 벤치, 열차, 비행선, 차원문, 트로피, 불꽃놀이와 지역 건물 이미지.
- `!도시꾸미기`의 드롭다운에서 부품을 선택하고 방향 버튼으로 좌표를 조절한다.
- 적용 전 장식 목록을 최대 10개 복구 지점으로 저장한다.
- 한 서버의 장식은 다른 서버와 완전히 분리한다.

## 3. 행동 연출 엔진
- 기존 게임 계산을 바꾸지 않고 `on_command`·`on_command_completion` 이벤트에서 연출 메시지만 추가한다.
- 채집, 제작, 보스, 탐험, 건설, 거래를 시작 → 진행 → 판정 → 완료 단계로 같은 메시지에서 갱신한다.
- Discord 컴포넌트의 `emoji=` 필드는 사용하지 않고 검증된 Unicode 이모지를 일반 텍스트로만 표시한다.
- 서버 관리자는 `off`, `compact`, `cinematic` 중 하나를 선택할 수 있다.
- 원래 명령이 실패하면 연출 메시지는 실패 상태로 바뀌고 기존 오류 처리기가 상세 원인을 안내한다.

## 4. 차원 항해·캠페인
- 7개 차원 템플릿과 주간 결정형 탐사 좌표를 제공한다.
- 차원 진행도, 참여자, 발견 기록과 클리어 계보를 서버 데이터에 저장한다.
- 선택형 캠페인은 전투·협상·탐사 선택을 기록하고 9회 선택 뒤 결말을 영구 기록한다.
- 서버 공동 우주선과 차원 기지는 시설 레벨과 XP를 유지한다.

## 5. 크루·공격대
- 크루 창설, 임무, 경험치와 레벨을 서버별로 저장한다.
- 차원 공격대는 최대 HP, 4개 페이즈, 참여자 피해·지원 점수와 최종 격파 계보를 기록한다.
- 보스 공격 보상은 기존 사용자 잔액 필드를 사용하며 최종 타격에서 한 번만 처리한다.

## 6. 선택형 서버 연합망
- 기본값은 꺼짐이다.
- 양쪽 서버가 연합망을 켠 뒤 소유 서버가 초대를 만들고 대상 서버 관리자가 직접 승인해야 가입된다.
- 공개되는 값은 서버 ID, 서버 표시명, 우주선·탐사 집계 점수뿐이며 개인 사용자 데이터는 교환하지 않는다.

## 7. 사용자 창작센터와 교환소
- 관리자만 퀘스트·보스 초안을 만들고 공개할 수 있다.
- 연합망과 콘텐츠 공유를 모두 켠 서버의 공개 콘텐츠만 전역 교환소에 등록된다.
- 설치는 원본을 수정하지 않는 복제 방식이며 설치 출처와 버전을 기록한다.

## 8. 리플레이·계보
- 공개 경기 요약만 저장하며 개인 손패는 포함하지 않는다.
- 차원 발견, 캠페인 결말, 공격대 격파와 도시 사진을 영구 계보와 세계 연표에 기록한다.

## 9. 문맥형 대화
- 기존 서버 승인 기억을 최우선으로 유지한다.
- 승인 기억과 일치하지 않을 때 v15 문맥 엔진이 이전 사용자 메시지, 이전 아바돈 답변과 대화 주제를 활용한다.
- 대화 세션은 60분, 최대 100턴, 최근 20개 대화 쌍을 유지한다.
- 짧은 `응`, `아니`, `왜`, `그거` 같은 후속 문장도 이전 주제에서 이어간다.
- 한국어와 English를 메시지별로 감지하고 한 답변 안에서 두 언어를 섞지 않는다.
- 관리자는 `!대화모드 문맥형` 또는 `!chatmode classic`으로 전환할 수 있다.

## 10. 안전·호환성
- v13.3 명령 등록 보호기 다음, 영문 별칭 최종 동기화 이전에 v15 모듈을 등록한다.
- 기존 `도시지도`만 명시적으로 제거한 뒤 새 콜백으로 교체한다.
- v15 신규·교체 명령 49개, 신규 접근 이름 138개는 v13.3 매니페스트와 충돌하지 않는다.
- 기존 BLACK CITY, 화투, 포커, 경마, 복구, 알림과 권한 저장 구조는 삭제하거나 초기화하지 않는다.
