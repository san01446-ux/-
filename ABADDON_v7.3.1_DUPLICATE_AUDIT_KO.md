# ABADDON v7.3.1 중복 기능 감사 보고서

## 결론

이번 패치에서는 **어떤 명령어·기능·데이터도 폐기하지 않았습니다.** 겹쳐 보이는 기능은 실제 내부 로직 공유 여부와 사용 목적을 구분해 정리했습니다. 폐기 가능성이 있는 항목은 후보로만 표시하며, 승인 전에는 삭제하거나 비활성화하지 않습니다.

## 1. 유지 확정

### 명령어 진입 화면

- `!명령어`
- `!도움말`
- `!귀여운메뉴`
- `!게임`

겉보기에는 비슷하지만 역할이 다릅니다. `!명령어`와 `!도움말`은 검색·설명 중심, `!귀여운메뉴`는 버튼 실행 중심, `!게임`은 RPG 기능군 중심입니다. 현재는 같은 대표 브라우저와 실행 파이프라인을 재사용하므로 삭제 없이 유지합니다.

### 신규 멤버 설정

- `!환영채널`
- `!인삿말설정`
- `!자동역할`
- `!새싹설정`

예전 명령어는 기존 서버 호환을 위해 남아 있지만, 실제 저장 대상은 통합 환영·역할 설정으로 연결됩니다. 기능이 두 번 실행되는 구조가 아니므로 호환 명령으로 유지합니다.

### 채널 안내

- `!채널규칙`: 현재 채널의 개별 안내 설치·갱신
- `!채널가이드`: 여러 채널의 전용 안내 일괄 설치

대상 범위가 달라 둘 다 유지합니다.

### 미니게임

- 기존 사람 간 멀티게임
- `!아바돈게임` AI 1:1

사람 대전과 혼자 하는 AI 대전으로 목적이 달라 유지합니다. 재화 정산과 게임 통계도 분리되어 있습니다.

## 2. 승인 전 폐기 후보

아래 항목은 **현재 삭제되지 않았습니다.** 실제 실서버 데이터 사용량과 영향 범위를 확인한 후 별도 승인이 필요합니다.

1. 통합 환영 설정과 동일 값을 복제 보관하는 `server_management` 구형 호환 키
2. 월드보스 v7.0 이전의 전역 상태 키 중 마이그레이션 이후에도 남아 있는 값
3. 패키지 루트에 누적된 과거 버전 설명·감사 문서
4. 대표 메뉴로 연결되지만 직접 사용량이 극히 낮은 호환 별칭

## 3. 자동으로 폐기하지 않는 이유

- 구버전 서버 설정을 사용하는 서버가 존재할 수 있음
- 홈페이지·고정 안내·외부 문서에서 과거 명령어를 참조할 수 있음
- 데이터 키 삭제 시 되돌리기와 구버전 복구가 어려워질 수 있음
- 별칭 제거는 이용자 경험과 자동화 스크립트를 깨뜨릴 수 있음

## 4. 이번 검사 결과

- 최상위 명령어 등록 지점: 523개
- 정적 명령어·별칭 이름: 797개
- 해결되지 않은 명령 이름 충돌: 0건
- 코어 `@bot.event` 중복: 0건
- 봇 소스 파일 삭제: 0건
- 홈페이지 파일 삭제: 0건
- 이미지 변경: 0장
- 실제 폐기: 0건

영문 `!help`는 등록 전에 기존 한국어 도움말의 영문 별칭을 안전하게 해제한 뒤 영어 전용 화면으로 재등록하는 의도된 런타임 교체입니다.
