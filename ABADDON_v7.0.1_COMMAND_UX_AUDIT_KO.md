# ABADDON v7.0.1 명령어 UX 감사 보고서

## 결론
기존 기능을 삭제하지 않고 **대표 진입점 + 기능군 + 검색** 구조로 정리했습니다. 처음 이용자는 `!처음`, `!게임`, `!명령어` 세 가지만 기억하면 되고, 기존 이용자의 세부 명령어와 영어 별칭은 그대로 작동합니다.

## 전체 점검 결과
- 직접 `bot.command` 계열 등록 지점: **490개**
- 별칭을 포함한 직접 등록 이름: **702개**
- 정적 분석상 직접 등록 이름 중복: **7개 이름**
- 게임센터 등록 기능: **227개**
- 게임센터 카테고리: **12개**
- 새 기능군: **33개**
- 가장 큰 기능군: **14개 항목**
- 홈페이지 명령 카드: **275개**

## 발견한 핵심 문제
### 1. Discord Select 25개 제한 초과
기존 게임센터는 장비 카테고리 **26개**, 생활 카테고리 **35개**를 한 드롭다운에 넣을 수 있는 구조였습니다. Discord Select의 옵션 상한을 넘기므로 일부 실행 환경에서 메뉴 생성 실패 또는 누락 위험이 있었습니다.

**수정:** 카테고리 → 기능군 → 실행 항목의 2단계 탐색으로 분리했습니다. 모든 기능군은 최대 14개입니다.

### 2. 처음 이용자에게 전체 기능이 한꺼번에 노출
기존 도움말과 홈페이지는 많은 명령을 바로 나열해 “무엇부터 해야 하는지” 판단하기 어려웠습니다.

**수정:**
- `!처음`: 첫날 5단계
- `!게임`: 처음 시작·오늘 추천·목적별 경로
- `!명령어`: 대표 명령 우선, 전체 목록 토글, 검색
- 홈페이지: 목적별 필터와 명령 복사

### 3. 비슷한 기능이 여러 명령어로 분산
정적 키워드 점검에서 다음 묶음이 확인됐습니다.

| 기능군 | 관련 등록 지점 수 | 정리 방식 |
|---|---:|---|
| 월드보스 | 16 | 전투·보상 / 기록·도감 / 관리자 테스트로 분리 |
| 알림 설정·해제·상태 | 11 | `!알림설정` 대표 메뉴 중심 |
| 은행·대출·사채 | 10 | 경제 기능군 아래 통합 |
| 카지노·미니게임 | 31 | 로비·보상 / 개별 게임으로 분리 |
| 서버 관리·설정·백업 | 55 | `!설정`, `!서버리뉴얼` 중심 |
| 상태 확인 | 27 | 각 콘텐츠 기능군 안에 배치 |
| 목록·도감 | 39 | 장비·펫·스토리 등 해당 콘텐츠 안에 배치 |

숫자는 명령 데코레이터의 이름과 별칭을 키워드로 점검한 결과이며, 같은 그룹의 하위 명령과 슬래시/프리픽스 병행 경로가 포함될 수 있습니다.

## 중복 이름 감사
직접 `bot.command` 계열에서 정적으로 겹치는 이름은 다음 교체·언어 호환 경로입니다.

- `help`: 한국어 `!도움말`의 별칭과 영어 `!help`가 겹칩니다. 영어 모듈 등록 과정에서 충돌 별칭을 정리하는 기존 호환 로직을 유지했습니다.
- `인카운트도감`, `조우도감`, `랜덤이벤트도감`: v6.3.1 구현을 v6.3.2 구현이 교체하며 `remove_command` 후 등록합니다.
- `선물거래`, `감염선물`, `레버리지`: v6.3.8 구현을 v6.4.0 인터랙티브 구현이 교체하며 기존 명령을 제거한 뒤 등록합니다.

새로 추가한 `처음`, `초보`, `초보가이드`, `시작가이드`는 각각 등록 경로가 1개뿐입니다.

## 호환성 정책
- 세부 명령어 삭제 없음
- 영어 별칭 삭제 없음
- 저장 데이터 구조 변경 없음
- 기존 쿨다운과 보상 계산 유지
- 도움말과 게임센터의 **표시·탐색 계층만** 재구성

## 검증
- Python 56개 파일 전체 컴파일 통과
- JavaScript 문법 검사 통과
- HTML 3개 파일 로컬 링크 검사 통과
- 게임 기능 227개가 기능군에 정확히 한 번씩 배정됨
- 누락 0, 알 수 없는 키 0, 중복 배정 0
- 기능군 최대 옵션 14개
- 홈페이지 명령 데이터 275개, `!처음`·`!게임`·`!명령어` 설명 반영
