ABADDON v6.3.7a 장비 모듈 시작 오류 핫픽스

원인
- apocalypse_bot/commands/v633_equipment_crafting.py 261행에 잘못 삽입된 `ebp(image)` 최상위 호출이 남아 있었습니다.
- 모듈 import 순간 정의되지 않은 이름을 실행해 NameError가 발생했고, 봇이 명령 등록 전에 종료됐습니다.

수정
- 잘못된 `ebp(image)` 호출 완전 제거
- `!패치노트` 및 `!테스트` 표시 버전을 v6.3.7a로 갱신
- 기존 v6.3.7 기능·데이터·확률·이미지에는 변경 없음

검수
- Python 49개 파일 문법 컴파일 통과
- v633_equipment_crafting.py 최상위 실행 호출 AST 검사 통과(0건)
- 전체 프로젝트 내 `ebp(image)` 잔존 문자열 0건
- ZIP 무결성 검사 통과

배포
- 전체본: 저장소 전체를 덮어쓰기
- 패치본: 저장소 루트에 압축 해제하여 덮어쓰기
